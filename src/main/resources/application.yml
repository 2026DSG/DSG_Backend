spring:
  datasource:
    url: jdbc:mysql://localhost:3306/DSG?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: crudkms


  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

jwt:
  secretKey: ${JWT_SECRET_KEY}
  accessExp: ${ACCESS_EXP}
  refreshExp: ${REFRESH_EXP}
  header: ${HEADER}
  prefix: ${PREFIX}
